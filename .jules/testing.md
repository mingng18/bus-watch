## 2024-06-19 - Testing `findNearbyBusRoutes`
**Learning:** Learned how to correctly test geographical location logic like distances checking edge cases (e.g. out of radius items, defaults radius parameter) and grouping identical entries (deduplication mapping by route or trip key).
**Action:** Implemented robust and explicit Vitest assertions within `backend/test/nearby.test.ts`.
