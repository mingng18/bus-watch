## 2024-05-24 - Precomputing maps for faster lookups
**Learning:** O(N) array find operations within loops create O(N^2) bottlenecks when working with trips or routes
**Action:** Use Map objects created before loops for O(1) lookups to improve performance
